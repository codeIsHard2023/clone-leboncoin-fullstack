// import { useMutation } from "@apollo/client";
// import { UPDATE_AD } from "../api/ads";
import { CreateUpdateForm } from "../components/createUpdateForm";

export const UpdateAdEditor = () => {
  //   const [doUpdateAd, { loading }] = useMutation(UPDATE_AD);

  return (
    <>
      <CreateUpdateForm />
    </>
  );
};
